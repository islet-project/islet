[workspace]
resolver = "2"
members = [
    "lib/uart",
    "realm/hvc-loop",
    "rmm/armv9a",
    "rmm/board/fvp",
    "rmm/monitor",
]

[profile.dev]
panic = "abort"

[profile.release]
lto = true
panic = "abort"
