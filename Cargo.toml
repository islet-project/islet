[workspace]
resolver = "2"
members = [
    "lib/cose",
    "lib/rsi-el0",
    "lib/uart",
    "realm/rsi-test",
    "rmm/armv9a",
    "rmm/board/fvp",
    "rmm/monitor",
    "sdk",
]

[profile.dev]
panic = "abort"

[profile.release]
lto = true
panic = "abort"
