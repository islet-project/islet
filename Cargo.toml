[workspace]
resolver = "2"
members = [
    "rmm/armv9a",
    "rmm/board/fvp",
    "rmm/monitor",
]

exclude = [
    "sdk",
    "sdk/bootstrap",
]

[profile.dev]
panic = "abort"

[profile.release]
lto = true
panic = "abort"
