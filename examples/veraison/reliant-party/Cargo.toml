[package]
name = "reliant-party"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
clap = {version = "*", features = ["derive"]}
chrono = "*"
env_logger ="*"
hex = "*"
log = "*"
tinyvec = "*"
rust-rsi = { path = "../../../lib/rust-rsi" }
ratls = { path = "../../../lib/ratls/ratls" }
realm-verifier = { path = "../../../lib/realm-verifier" }
veraison-verifier = { path = "../../../lib/ratls/veraison-verifier" }
