[package]
name = "utrace_common"
version = "0.1.0"
authors = ["Islet Contributors"]
edition = "2021"

build = "../build.rs"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
