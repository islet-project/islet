[package]
name = "islet-sdk"
version = "0.1.0"
authors = ["Islet Contributors"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[build-dependencies]
cbindgen = "0.24.0"

[dependencies]
minicbor = "*"
bincode = "1.0"
serde = { version = "1.0", features = ["derive"] }
